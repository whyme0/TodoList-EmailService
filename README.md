# TodoList-EmailService
EmailService manage emails for TodoList web-app. This is an obligatory external project that TodoList web application needs for some parts of the application to work properly.
